import React, { useState } from 'react';
import { StyleSheet, Button,Text, View,} from 'react-native';

const Button_light_green = () => {
    return (
    <View>
     <View> 
       <Button title="Commencer" onPress={() => {}} color="#73eca6" ></Button> 
    </View> 
    </View>
    )}

 export default Button_light_green;
